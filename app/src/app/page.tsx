import React, { useState } from "react";

export default function Home() {
  return (
    <div>
      <h1>SALARY TIMER</h1>
      <div>
        <button>Start</button>
        <button>Stop</button>
        <button>Reset</button>
      </div>
    </div>
  );
}
